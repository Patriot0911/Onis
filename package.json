{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web:dev": "npx lerna run dev --scope=web",
    "api:dev": "npx lerna run dev --scope=api"
  },
  "dependencies": {
    "react-icons": "^5.3.0",
    "root": "file:"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
