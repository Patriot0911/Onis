import InnerLayout from '@/components/InnerLayout';

const CollectionsPage = () => {
    return (
        <InnerLayout>
            <div className="flex justify-center items-center">Сollections</div>
        </InnerLayout>
    );
};

export default CollectionsPage;
